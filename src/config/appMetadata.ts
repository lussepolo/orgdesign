// V10-X2T: single presentation source for application name/version/
// environment labels. Previously "v3.0" (header ×2, AboutModal footer) and
// "2.5 Stable Release" (Cover version card) conflicted — this reuses the
// existing, more current "v3.0" marker rather than inventing a new release
// number, and removes the stale "2.5 Stable Release" string.
export const APP_VERSION_LABEL = "v3.0";
export const APP_ABOUT_SEEN_STORAGE_KEY = "hasSeenAbout_v3.0";
